**Docker Page Speedup**

Makes the Unraid Docker page load faster and cuts its CPU/GPU load on systems with many containers. It caches container-template reads in the webGUI and lets you throttle how often the page refreshes live container stats. Configure it under **Settings → Utilities → Docker Page Speedup**, or click this plugin's bolt icon.
